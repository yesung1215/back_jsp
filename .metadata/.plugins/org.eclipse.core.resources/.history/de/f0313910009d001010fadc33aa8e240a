<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %> 
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>jstl</title>
</head>
<body>
	<h1>jstl</h1>
	
<%-- <c:if test="${name} != ''">
		<h2>${name}</h2>
	</c:if>
	<c:if test="${empty param.name}">
		<h2>비어있니?</h2>
	</c:if>
	<c:if test="${not empty param.name}">
		<h2>비어 있지 않아</h2>
	</c:if>
	<h2>${name}</h2> 
	<h3>😁</h3> 
--%>

<%-- 	
		<c:choose>
			<c:when test="${empty name}">
				<h1>비회원</h1>
			</c:when>
			<c:otherwise>
				<h1>${name}님</h1>
			</c:otherwise>
		</c:choose>
 --%>

<!-- 	이름과 나이를 입력 받아, 이름과 나이와 만나이를 출력하기
	값이 없다면 "정보를 입력하세요"를 출력하기 -->
	
	<c:choose>
		<c:when test="${empty name || age}">
			<h2>정보를 입력하세요</h2>
		</c:when>
		<c:otherwise>
			<h2>이름:${name}</h2>
			<h2>나이:${age}</h2>
			<h2>만 나이:${age} - ${1}</h2>
		</c:otherwise>
	</c:choose> 






</body>
</html>